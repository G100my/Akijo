
/**
 * @type {import("tailwindcss").Config};
 */
const config = {
  theme: {
    extend: {
      colors: {
        design: {
          red: '#ff4c14',
        },
      },
    },
  },
}

module.exports = config
